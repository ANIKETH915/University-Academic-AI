# Universal PYQ Intelligence — backend dependencies
# Python 3.11+ (validated on 3.13)

# API layer
fastapi==0.135.2
uvicorn==0.42.0
python-multipart==0.0.22
pydantic==2.12.5

# PDF text extraction
PyMuPDF==1.26.6

# OCR fallback (requires the Tesseract binary installed on the host)
pytesseract==0.3.13
pillow==12.2.0

# Embeddings + vector store
sentence-transformers==5.3.0
chromadb==1.5.9
numpy==2.3.5



# HTTP + tests
requests==2.32.5
pytest==9.1.1
